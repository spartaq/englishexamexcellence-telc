# Plan: Relayout Level Hubs to Emphasize Vocab and Drills

## Status: IN PROGRESS (revised layout)

## Core Messaging
"You could do just a bunch of mock tests, but that's not so useful. What you really need is to increase passive vocabulary and to practice some skills related to the test but which are also just generally useful."

## Changes Implemented

### 1. BrandTestHub.jsx ✓
- Added "teacher-note" message box in hero: "The Real Solution" narrative
- Moved Vocab/Drills tools to "The Real Solution" section (immediately visible)
- Updated tool descriptions

### 2. DrillsHub.jsx ✓
- Added tagline: "Skills that serve you beyond the exam"
- Added category descriptions

### 3. VocabHub.jsx ✓
- Added tagline: "Passive vocabulary = reading fluency + comprehension"

### 4. EXTRA_TOOLS descriptions (App.jsx) ✓
- Updated Vocab/Drills descriptions

### 5. CSS Styles ✓
- Added `.drills-hub-tagline` style
- Added `.vocab-hub-tagline` style
- Added `.category-description` style

## REVISED REQUIREMENT: Side-by-Side Layout
**Pending changes:**
- Combine "Check Progress" and "Skill Practice" into side-by-side boxes
- Both boxes same size
- Mock tests scroll inside "Check Progress" box
- Desktop: 2-column grid
- Mobile: 1-column stack (single column)

### Implementation Plan for Side-by-Side Layout:

#### BrandTestHub.jsx Changes:
1. Wrap "Check Progress" and "Skill Practice" in a new container div with class "dual-section-grid"
2. Move both `quick-start-grid` sections into this container
3. Each section gets class "dual-section-box"

#### BrandTestHub.css Changes:
1. Add `.dual-section-grid` with:
   - `display: grid; grid-template-columns: 1fr 1fr; gap: 24px;`
2. Add `.dual-section-box` with:
   - `display: flex; flex-direction: column; height: 100%`
3. Add fixed height to `.exam-list` with:
   - `max-height: 400px; overflow-y: auto;`
   - Add custom scrollbar styling
4. Add responsive media query:
   - `@media (max-width: 768px) { .dual-section-grid { grid-template-columns: 1fr; } }`